package main

import (
	"fmt"
	"net/http"
	"net/http/httptest"
)

// Kirish — ro'yxatga olish uchun yuboriladigan ma'lumot.
type Kirish struct {
	Ism string `json:"ism"`
}

// royxatgaOl ism'ni JSON qilib url'ga POST orqali yuboradi, status
// kodini qaytaradi.
func royxatgaOl(url, ism string) (int, error) {
	// TODO: Kirish{Ism: ism} ni JSON'ga aylantiring, http.Post orqali yuboring
	return 0, nil
}

func main() {
	testServer := httptest.NewServer(http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		w.WriteHeader(http.StatusCreated)
	}))
	defer testServer.Close()

	status, _ := royxatgaOl(testServer.URL, "Ali")
	fmt.Println(status)
}
