package main

import "fmt"

func pairsWithSumK(N int, K int, arr []int) int {
	count := 0
	for i := 0; i < N; i++ {
		for j := i + 1; j < N; j++ {
			if arr[i]+arr[j] == K {
				count++
			}
		}
	}
	return count
}

func main() {
	fmt.Println(pairsWithSumK(5, 10, []int{2, 8, 5, 3, 7}))
}
