package main

import (
	"math"
	"testing"
)

func TestSolveCase1(t *testing.T) {
	got := solve(5)
	var want float64 = 20.0
	if math.Abs(got-want) > 1e-6 {
		t.Errorf("solve(a=5):\nExpected:\n%v\n\nGot:\n%v", want, got)
	}
}
func TestSolveCase2(t *testing.T) {
	got := solve(3.5)
	var want float64 = 14.0
	if math.Abs(got-want) > 1e-6 {
		t.Errorf("solve(a=3.5):\nExpected:\n%v\n\nGot:\n%v", want, got)
	}
}
func TestSolveCase3(t *testing.T) {
	got := solve(10)
	var want float64 = 40.0
	if math.Abs(got-want) > 1e-6 {
		t.Errorf("solve(a=10):\nExpected:\n%v\n\nGot:\n%v", want, got)
	}
}
