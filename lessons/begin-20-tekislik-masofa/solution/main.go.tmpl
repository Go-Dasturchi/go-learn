package main

import (
	"fmt"
	"math"
)

func solve(x1 int, y1 int, x2 int, y2 int) float64 {
	dx := float64(x2 - x1)
	dy := float64(y2 - y1)
	return math.Sqrt(dx*dx + dy*dy)
}

func main() {
	fmt.Println(solve(0, 0, 3, 4))
}
