package main

import "fmt"

func exactlyOnePositive(A int, B int, C int) bool {
	count := 0
	if A > 0 {
		count++
	}
	if B > 0 {
		count++
	}
	if C > 0 {
		count++
	}
	return count == 1
}

func main() {
	fmt.Println(exactlyOnePositive(-1, 5, -3))
}
