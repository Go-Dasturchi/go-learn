package main

import (
	"fmt"
	"strings"
)

func solve(word1 []string, word2 []string) bool {
	return strings.Join(word1, "") == strings.Join(word2, "")
}

func main() {
	fmt.Println(solve([]string{"ab", "c"}, []string{"a", "bc"}))
}
