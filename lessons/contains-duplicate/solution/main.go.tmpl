package main

import "fmt"

func solve(nums []int) bool {
	seen := make(map[int]bool, len(nums))
	for _, n := range nums {
		if seen[n] {
			return true
		}
		seen[n] = true
	}
	return false
}

func main() {
	fmt.Println(solve([]int{1, 2, 3, 1}))
}
