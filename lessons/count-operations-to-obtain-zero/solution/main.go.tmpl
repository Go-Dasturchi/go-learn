package main

import "fmt"

func solve(num1 int, num2 int) int {
	count := 0
	for num1 != 0 && num2 != 0 {
		if num1 >= num2 {
			num1 -= num2
		} else {
			num2 -= num1
		}
		count++
	}
	return count
}

func main() {
	fmt.Println(solve(2, 3))
}
