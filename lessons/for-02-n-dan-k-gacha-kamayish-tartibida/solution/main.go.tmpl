package main

import (
	"fmt"
	"strconv"
)

func rangeNtoK(K int, N int) string {
	result := ""
	for i := N; i >= K; i-- {
		if i < N {
			result += " "
		}
		result += strconv.Itoa(i)
	}
	return result
}

func main() {
	fmt.Println(rangeNtoK(3, 7))
}
