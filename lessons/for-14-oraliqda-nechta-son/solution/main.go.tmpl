package main

import "fmt"

func countInRange(A int, B int, N int, nums []int) int {
	count := 0
	for i := 0; i < N && i < len(nums); i++ {
		if nums[i] >= A && nums[i] <= B {
			count++
		}
	}
	return count
}

func main() {
	fmt.Println(countInRange(5, 10, 7, []int{3, 7, 12, 8, 2, 9, 6}))
}
