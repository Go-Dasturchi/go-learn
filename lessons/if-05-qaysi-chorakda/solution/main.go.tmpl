package main

import "fmt"

func getQuadrant(x int, y int) int {
	if x > 0 && y > 0 {
		return 1
	} else if x < 0 && y > 0 {
		return 2
	} else if x < 0 && y < 0 {
		return 3
	}
	return 4
}

func main() {
	fmt.Println(getQuadrant(5, 3))
}
