package main

import "fmt"

func solveQuadratic(A int, B int, C int) int {
	discriminant := B*B - 4*A*C
	if discriminant > 0 {
		return 2
	} else if discriminant == 0 {
		return 1
	}
	return 0
}

func main() {
	fmt.Println(solveQuadratic(1, -3, 2))
}
