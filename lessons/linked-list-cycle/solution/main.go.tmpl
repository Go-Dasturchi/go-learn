package main

import "fmt"

func solve(next []int, start int) bool {
	visited := make(map[int]bool)
	cur := start
	for cur != -1 {
		if visited[cur] {
			return true
		}
		visited[cur] = true
		cur = next[cur]
	}
	return false
}

func main() {
	fmt.Println(solve([]int{1, 2, 0, 3}, 0))
}
