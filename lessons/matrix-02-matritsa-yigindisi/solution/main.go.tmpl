package main

import "fmt"

func matrixSum(M int, N int, matrix [][]int) int {
	sum := 0
	for i := 0; i < M; i++ {
		for j := 0; j < N; j++ {
			sum += matrix[i][j]
		}
	}
	return sum
}

func main() {
	fmt.Println(matrixSum(2, 3, [][]int{[]int{1, 2, 3}, []int{4, 5, 6}}))
}
