package main

import "fmt"

func solve(nums []int) int {
	best, cur := 0, 0
	for _, n := range nums {
		if n == 1 {
			cur++
			if cur > best {
				best = cur
			}
		} else {
			cur = 0
		}
	}
	return best
}

func main() {
	fmt.Println(solve([]int{1, 1, 0, 1, 1, 1}))
}
