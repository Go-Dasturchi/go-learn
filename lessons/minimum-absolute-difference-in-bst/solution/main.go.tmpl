package main

import "fmt"

func solve(nums []int) int {
	if len(nums) < 2 {
		return 0
	}
	min := nums[1] - nums[0]
	for i := 2; i < len(nums); i++ {
		if d := nums[i] - nums[i-1]; d < min {
			min = d
		}
	}
	return min
}

func main() {
	fmt.Println(solve([]int{1, 3, 6, 9, 12}))
}
