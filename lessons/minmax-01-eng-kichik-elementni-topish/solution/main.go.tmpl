package main

import "fmt"

func findMin(N int, nums []int) int {
	min := nums[0]
	for i := 1; i < N; i++ {
		if nums[i] < min {
			min = nums[i]
		}
	}
	return min
}

func main() {
	fmt.Println(findMin(5, []int{3, 7, 2, 9, 1}))
}
