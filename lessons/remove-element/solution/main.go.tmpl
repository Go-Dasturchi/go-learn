package main

import "fmt"

func solve(nums []int, val int) int {
	k := 0
	for i := 0; i < len(nums); i++ {
		if nums[i] != val {
			nums[k] = nums[i]
			k++
		}
	}
	return k
}

func main() {
	fmt.Println(solve([]int{3, 2, 2, 3}, 3))
}
