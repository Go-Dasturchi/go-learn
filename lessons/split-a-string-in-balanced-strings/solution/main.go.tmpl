package main

import "fmt"

func solve(s string) int {
	count := 0
	balance := 0
	for _, c := range s {
		if c == 'L' {
			balance++
		} else {
			balance--
		}
		if balance == 0 {
			count++
		}
	}
	return count
}

func main() {
	fmt.Println(solve("RLRRLLRLRL"))
}
